### 1️⃣ 用户提问（完整保留）

把这三项必填资料也同步到云端用户资料
  里，启动时优先从云端恢复，本地缓存只作为加速缓存

---

### 2️⃣ Codex 核心结论

- 问题根因：`身高(cm)`、`体重(kg)`、`每周可训练天数` 原本只保存在小程序本地缓存 `keepfit:ai:profile:v1`，用户长时间未打开、缓存被清理或换设备后会丢失；训练历史仍在云端，所以历史可见但 AI 必填资料需要重填。
- 决策：
  - 继续使用既有 `profile` 云函数和 `profile` 集合，不新增集合。
  - 三项 AI 必填资料写入云端用户资料。
  - 页面启动先读本地缓存快速显示，再读取云端资料覆盖三项必填字段并回写本地缓存。
  - 云端 `profile.set` 改为字段合并，避免 AI 资料保存时清空计划设置页已有资料。
- 实现位置：
  - `cloudfunctions/profile/index.js`：新增 `heightCm`、`weightKg`、`weeklyTrainingDays` 字段持久化，并保留未提交旧字段。
  - `miniprogram/pages/mine/index.js`：加载时从云端恢复三项，保存时同步云端。
  - `miniprogram/pages/progress/index.js`：AI 总结入口加载/保存时同步云端。
- 验证：
  - 新增 `cloudfunctions/profile/profile.test.js` 覆盖三项写入读回，以及只保存 AI 三项时保留既有 profile 字段。

---

### 3️⃣ 元信息

Generated by: Codex CLI  
Date: 2026-06-12  
Project: keepfit  
Topic: AI 必填资料云端同步  
