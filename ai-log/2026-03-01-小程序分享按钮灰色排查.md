### 1️⃣ 用户提问（完整保留）

我这个小程序发布后，分享按钮是灰色，据说可以修改代码主动实现分享功能，比如在页面代码中实现以下两个函数：

```javascript
onShareAppMessage()
onShareTimeline()
```

### 2️⃣ Codex 核心结论

- 当前仓库首页 `miniprogram/pages/workout-today/index.js` 未实现 `onShareAppMessage` 或 `onShareTimeline`。
- 当前仓库首页 `miniprogram/pages/workout-today/index.json` 仅包含导航标题，未看到页面级分享开关配置。
- 对于微信小程序，分享按钮是否可用通常不只取决于页面里是否写了函数，还取决于页面是否显式开启分享能力。
- 因此，“只加两个函数”不是完整修复；通常需要同时补页面 JSON 配置与页面 JS 的分享返回逻辑。
- 若目标是分享到朋友圈，还需要注意该能力有页面形态与平台限制；如果页面能力或场景不满足，按钮仍可能不可用。
- 当前项目 `spec / plan / tasks` 中未包含“分享能力”需求，按项目治理规则，不应直接改行为，除非先补充本次变更范围。

最终结论：

当前问题大概率是页面未声明分享能力，不能只靠发布后自然生效。最小方案应为：
1. 在目标页面 `index.json` 开启分享相关配置。
2. 在目标页面 `index.js` 实现 `onShareAppMessage`，如需朋友圈再实现 `onShareTimeline`。
3. 再在真机和线上版本验证右上角菜单是否点亮。

补充执行结果：

- 已在 `miniprogram/pages/workout-today/index.json` 开启 `enableShareAppMessage` 与 `enableShareTimeline`。
- 已在 `miniprogram/pages/workout-today/index.js` 增加分享常量、`buildSharePayload()`、`onShareAppMessage()`、`onShareTimeline()`。
- 分享回流主路径统一指向 `/pages/workout-today/index`。
- 已通过本地静态解析验证分享页面 `json/js` 语法正常。
- `spec.md` 与 `plan.md` 已补充本次 CR-21 变更说明。
- `tasks.md` 因文件编码不是有效 UTF-8，当前无法通过 `apply_patch` 安全修改，需后续先修复编码再补任务状态。

后续扩展结果：

- 已将 CR-21 范围从单页扩展到三个 Tab 页面：`workout-today`、`progress`、`mine`。
- 已在 `miniprogram/pages/progress/index.json` 与 `miniprogram/pages/mine/index.json` 开启分享配置。
- 已在 `miniprogram/pages/progress/index.js` 与 `miniprogram/pages/mine/index.js` 实现 `onShareAppMessage()`、`onShareTimeline()`。
- “进度”页面分享回流路径为 `/pages/progress/index`；“我的”页面分享回流路径为 `/pages/mine/index`。
- 已再次通过本地静态解析验证这两个页面的 `json/js` 语法正常。

### 3️⃣ 元信息

Generated by: Codex CLI
Date: 2026-03-01
Project: keepfit
Topic: 小程序分享按钮灰色排查
