### 1️⃣ 用户提问（完整保留）

是否我的账号是授权了，所以应该是“登录”状态，如果没有登录的话，你核实一下，是否会出现“登录”的选择按钮

请按照这个思路进行修改

---

### 2️⃣ Codex 核心结论

- `auth.status=authorized` 应视为用户已授权登录，不能在页面上直接显示技术值 `authorized`。
- `nickName` 为空不等于未登录；微信资料可能为空，但登录态仍应以 `auth.status` 为准。
- “我的”页的展示规则：
  - `authorized`：用户名为空时显示“已登录用户”，账号状态显示“已登录”，不显示登录按钮。
  - `guest` 或无授权记录：用户名显示“未登录”，账号状态显示“未登录”，显示“微信一键登录”按钮。
  - 授权状态读取失败：显示“状态未知 / 登录状态读取失败”，不显示登录按钮，避免误导为用户未授权。
- 实现：
  - `miniprogram/pages/mine/index.js` 增加登录状态中文映射、展示名兜底和登录按钮跳转。
  - `miniprogram/pages/mine/index.wxml` 不再直接渲染 `profile.status`，改为 `profile.statusText`，并按 `profile.actionText` 显示登录 CTA。
  - `miniprogram/pages/mine/index.wxss` 增加小号登录按钮样式，沿用现有按钮风格。
  - `miniprogram/pages/mine/mine-ui.test.js` 增加静态回归测试，防止再次直接显示技术状态。

---

### 3️⃣ 元信息

Generated by: Codex CLI  
Date: 2026-06-12  
Project: keepfit  
Topic: 我的页登录状态展示与 CTA  
